from motor_import_modules_test_file1 import counter_1
#from motor_import_modules_test_file2 import bar_length_1
from raspi_tv_teurrent_bar_length = (max_range / 10)
		
		window.addstr(10, 10, s_bar(window):
	while True:
		
		current_counter = counter_1
		current_bar_length = (max_range / 10)
		
		window.addstr(10, 10, "[" + ("=" * current_counter) + ">" + (" " * (current_bar_length - current_counter)) + "]")
		window.refresh()
		time.sleep(0.5)
		
curses.wrapper(progress_bar)
